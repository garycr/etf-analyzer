# REV-053: WP-2 PostgreSQL Decimal Validation Review

**Date:** 2026-09-15
**Reviewer:** Code Reviewer dispatch using alternate model Claude Sonnet 5
**Scope:** PT-FIX-001F PostgreSQL Money precision correction and cumulative migration re-baseline
**Result:** PASS; aggregate PT-FIX-001F implemented, reviewed, and approved for bounded publication

## Technical Disposition

The alternate-model review found no Critical, Major, Minor, or code-quality defect in the bounded database implementation. The pre-cast Money grammar now permits the complete 20-integer-digit coefficient of `NUMERIC(28,8)` while rejecting scale and precision overflow before PostgreSQL can round. Both observation tables use class-specific limits of less than $10^{18}$ for Quantity and UnitPrice, $10^{20}$ for Money, and $10^{16}$ for Rate. These bounds agree with the DEC-014 precision-28 typmods and application-layer validation.

Executable vectors prove 18-, 19-, and 20-integer-digit Money admission, positive and negative maxima, market and economic persistence, exact textual readback, and rejection of 21-digit and excess-scale values. The affected migration chain passed 12/12, and both serial and CI-equivalent complete PostgreSQL-backed suites passed 248/248 with zero skips.

## Review Remediation

The initial review returned bounded technical PASS and kept aggregate PT-FIX-001F open on two evidence findings. First, the WP-1 migration artifacts retained historical test totals and did not record the 12/12 or 248/248 re-execution. Dated re-baseline verification sections now record the exact PostgreSQL baseline, boundary behavior, and test totals. Second, no dedicated review record existed for the database correction; this record and the matching WP-2 PostgreSQL gate evidence supply that trace.

The PostgreSQL contract labels the dated amendment. Sequence-4 SQL and cumulative sequence-4-through-6 manifest hashes were generated by PostgreSQL `16.15|C|UTF8|UTC|on`, pinned in executable tests, and synchronized across canonical evidence. Sequence 5 and 6 SQL bytes are unchanged. Existing journal entries retain their historical hashes under the append-only rule.

## Governance Boundary

Changing sequence 4 is permitted only because `v1.0.0-prototype.1` remains inactive, the repository documents empty-database installation only, and no released, deployed, production, or legacy migration ledger exists. Once a baseline is active or applied outside disposable test databases, the same correction requires an append-only successor migration and cannot rewrite sequence 4.

The final alternate-model recheck closed both prior Major evidence findings and returned PASS with no technical remediation remaining. The Workspace Owner approved aggregate PT-FIX-001F for bounded publication. PT-FIX-001G, K, N, and O, quality suppression, coverage, provider egress, complete WP-2, legacy migration, release, deployment, and production action remain outside this review.
